# MyBankApp

A simple banking application backed by Aiven PostgreSQL.

## Setup

1. Install dependencies: `npm install`
2. Configure your database connection in `.env`
3. Run: `npm start`

## Environment Variables

```
DATABASE_URL=postgres://...
```
